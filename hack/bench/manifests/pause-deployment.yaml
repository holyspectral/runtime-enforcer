apiVersion: apps/v1
kind: Deployment
metadata:
  name: pause-deployment
  namespace: ${namespace}
spec:
  replicas: ${replicas}
  selector:
    matchLabels:
      app: pause
  template:
    metadata:
      labels:
        app: pause
        security.rancher.io/policy: pause-policy
    spec:
      nodeName: ${node}
      containers:
      - name: pause
        image: registry.k8s.io/pause:3.10
        resources:
          limits:
            cpu: 10m
            memory: 32Mi
          requests:
            cpu: 10m
            memory: 32Mi
